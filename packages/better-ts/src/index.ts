/**
 * `better-ts` — meta package that re-exports the full public API of
 * {@link https://www.npmjs.com/package/@better-ts/core | @better-ts/core}.
 *
 * Prefer importing from `@better-ts/core` directly; this package exists so that
 * `npm i better-ts` works as a convenient entry point.
 */
export * from "@better-ts/core";
